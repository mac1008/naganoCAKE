<h1>編集先一覧</h1>

<%= form_with model: @book do |f| %>
  <p>郵便番号（ハイフンなし）</p>
  <%= f.text_field :title %>
  <p>住所</p>
  <%= f.text_field :title %>
  <p>宛名</p>
  <%= f.text_field :title %>
  <%= f.submit '変更内容を保存' %>
<% end %>
<!--仮-->